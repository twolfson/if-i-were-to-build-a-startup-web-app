# from django.urls import path

# from allauth.socialaccount import views as allauth_socialaccount_views

urlpatterns = [
    # Only expose URLs we want to at the beginning, https://github.com/pennersr/django-allauth/blob/0.54.0/allauth/account/urls.py  # noqa:E501
    # path(
    #     "login/cancelled/",
    #     allauth_socialaccount_views.login_cancelled,
    #     name="socialaccount_login_cancelled",
    # ),
    # path("login/error/", allauth_socialaccount_views.login_error, name="socialaccount_login_error"),
    # path("signup/", allauth_socialaccount_views.signup, name="socialaccount_signup"),
    # path("connections/", allauth_socialaccount_views.connections, name="socialaccount_connections"),
]
